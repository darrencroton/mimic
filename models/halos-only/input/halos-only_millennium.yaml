# Mimic halos-only Millennium Run Configuration

model:
  name: halos-only

simulation:
  name: millennium

input:
  # Subset of the 512 Millennium tree files
  first_file: 0
  last_file: 15

output:
  output_filename: halos
  output_directory: output/halos-only-millennium
  output_format: hdf5
  snapshot_list: [63, 37, 32, 27, 23, 20, 18, 16]

SubSteps: 1

modules:
  parameters: {}
