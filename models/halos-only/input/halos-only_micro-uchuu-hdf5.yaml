# Mimic halos-only micro-Uchuu (uchuutools forests-HDF5) Run Configuration

model:
  name: halos-only

simulation:
  name: micro-uchuu-hdf5

output:
  output_filename: halos
  output_directory: output/halos-only-micro-uchuu-hdf5
  output_format: hdf5
  snapshot_list: [49, 28, 23, 16, 12, 10, 8, 7]

SubSteps: 1

modules:
  parameters: {}
