# Mimic halos-only micro-Uchuu (L-Halo binary) Run Configuration

model:
  name: halos-only

simulation:
  name: micro-uchuu-lhalo

output:
  output_filename: halos
  output_directory: output/halos-only-micro-uchuu-lhalo
  output_format: hdf5
  snapshot_list: [49]                  # z=0 snapshot only (fast validation run)

SubSteps: 1

modules:
  parameters: {}
