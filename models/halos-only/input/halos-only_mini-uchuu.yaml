# Mimic halos-only mini-Uchuu (L-Halo binary) Run Configuration

model:
  name: halos-only

simulation:
  name: mini-uchuu

output:
  output_filename: halos
  output_directory: output/halos-only-mini-uchuu
  output_format: hdf5
  snapshot_list: [49, 28, 23, 16, 12, 10, 8, 7]

SubSteps: 1

modules:
  parameters: {}
