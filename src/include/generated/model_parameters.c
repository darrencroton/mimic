/* AUTO-GENERATED CODE - DO NOT EDIT
 *
 * Generated by: scripts/generate_model_parameters.py
 * Source: src/modules/.../module_info.yaml (parameter_definitions sections)
 * Metadata MD5: 7a9ae747146a8c00b8d5f21a6a8f6f3f
 *
 * To regenerate: make generate
 *
 * Architecture:
 *   - Each module defines its own parameters in module_info.yaml
 *   - Multiple modules can define same parameter (for shared parameters)
 *   - First module wins, type must match or generation fails
 *   - All parameters are REQUIRED in input file (no defaults)
 */

#include "generated/model_parameters.h"
#include "error.h"
#include <string.h>

/* Required parameter names (for startup validation) */
const char *REQUIRED_MODEL_PARAMETERS[NUM_REQUIRED_MODEL_PARAMETERS] = {
    "AGNrecipeOn",
    "BaryonFrac",
    "BlackHoleGrowthRate",
    "CoolFunctionsDir",
    "DiskInstabilityOn",
    "DiskRadiusFactor",
    "EnergySNcode",
    "EtaSNcode",
    "FeedbackEjectionEfficiency",
    "FeedbackReheatingEpsilon",
    "FracZleaveDisk",
    "QuasarModeEfficiency",
    "RadioModeEfficiency",
    "ReIncorporationFactor",
    "RecycleFraction",
    "SFprescription",
    "SfrEfficiency",
    "SupernovaRecipeOn",
    "TestFixtureDummyParameter",
    "TestFixtureEnableLogging",
    "ThreshMajorMerger",
    "Yield",
};

/* Parameter metadata (minimal schema - no range validation) */
const struct ModelParameterMetadata MODEL_PARAMETER_METADATA[NUM_REQUIRED_MODEL_PARAMETERS] = {
    { "AGNrecipeOn", "int", "AGN feedback mode (0=off, 1=empirical, 2=Bondi-Hoyle, 3=cold cloud accretion)", "dimensionless", "sage_cooling" },
    { "BaryonFrac", "double", "Cosmic baryon fraction (Omega_b / Omega_m)", "dimensionless", "sage_infall" },
    { "BlackHoleGrowthRate", "double", "Black hole growth efficiency during mergers", "dimensionless", "sage_mergers" },
    { "CoolFunctionsDir", "string", "Directory containing cooling function tables (metal-dependent cooling rates)", "path", "sage_cooling" },
    { "DiskInstabilityOn", "int", "Enable disk instability physics (0=off, 1=on)", "dimensionless", "sage_disk_instability" },
    { "DiskRadiusFactor", "double", "Factor relating disk effective radius to scale radius", "dimensionless", "sage_disk_instability" },
    { "EnergySNcode", "double", "Supernova energy per event in code units", "code_units", "sage_starformation_feedback" },
    { "EtaSNcode", "double", "Supernova rate per unit stellar mass formed", "code_units", "sage_starformation_feedback" },
    { "FeedbackEjectionEfficiency", "double", "Ejection efficiency in energy-driven wind formula", "dimensionless", "sage_mergers" },
    { "FeedbackReheatingEpsilon", "double", "Reheating efficiency - M_reheat = epsilon * M_stars", "dimensionless", "sage_mergers" },
    { "FracZleaveDisk", "double", "Fraction of newly produced metals that leave the disk in winds", "dimensionless", "sage_mergers" },
    { "QuasarModeEfficiency", "double", "Quasar-mode AGN feedback efficiency", "dimensionless", "sage_mergers" },
    { "RadioModeEfficiency", "double", "Efficiency of radio-mode AGN feedback (fraction of accretion energy coupled to gas heating)", "dimensionless", "sage_cooling" },
    { "ReIncorporationFactor", "double", "Multiplicative factor for reincorporation timescale", "dimensionless", "sage_reincorporation" },
    { "RecycleFraction", "double", "Fraction of stellar mass immediately recycled to ISM", "dimensionless", "sage_mergers" },
    { "SFprescription", "int", "Star formation law (0=Kennicutt-Schmidt with density threshold)", "dimensionless", "sage_starformation_feedback" },
    { "SfrEfficiency", "double", "Star formation efficiency (epsilon_SF in SFR = epsilon_SF * M_cold / t_dyn)", "dimensionless", "sage_starformation_feedback" },
    { "SupernovaRecipeOn", "int", "Enable supernova feedback (0=off, 1=on)", "dimensionless", "sage_mergers" },
    { "TestFixtureDummyParameter", "double", "Test parameter for infrastructure testing only", "dimensionless", "test_fixture" },
    { "TestFixtureEnableLogging", "int", "Enable verbose logging in test_fixture module", "dimensionless", "test_fixture" },
    { "ThreshMajorMerger", "double", "Mass ratio threshold for major mergers", "dimensionless", "sage_mergers" },
    { "Yield", "double", "Metal yield per unit stellar mass formed", "dimensionless", "sage_mergers" },
};

/* Simplified validation: check parameter exists (no range checking) */
int validate_model_param_double(const char *param_name, double value) {
    (void)value;  /* Unused - no range validation */
    const struct ModelParameterMetadata *meta = get_model_param_metadata(param_name);
    if (meta == NULL) {
        ERROR_LOG("Model parameter '%s' not found in metadata", param_name);
        return -1;
    }
    return 0;  /* No range validation - trust the user */
}

int validate_model_param_int(const char *param_name, int value) {
    (void)value;  /* Unused - no range validation */
    const struct ModelParameterMetadata *meta = get_model_param_metadata(param_name);
    if (meta == NULL) {
        ERROR_LOG("Model parameter '%s' not found in metadata", param_name);
        return -1;
    }
    return 0;  /* No range validation - trust the user */
}

const struct ModelParameterMetadata *get_model_param_metadata(const char *param_name) {
    for (int i = 0; i < NUM_REQUIRED_MODEL_PARAMETERS; i++) {
        if (strcmp(MODEL_PARAMETER_METADATA[i].name, param_name) == 0) {
            return &MODEL_PARAMETER_METADATA[i];
        }
    }
    return NULL;
}

/* Smart parameter lookup based on module dependencies */
int get_required_params_for_modules(
    const char **enabled_modules,
    int num_enabled,
    const char **required_params_out,
    int *num_required_out
) {
    if (num_enabled == 0) {
        *num_required_out = 0;
        return 0;
    }

    /* Build union of all required parameters */
    int num_required = 0;

    for (int i = 0; i < num_enabled; i++) {
        const char *module_name = enabled_modules[i];

        if (strcmp(module_name, "sage_cooling") == 0) {
            /* sage_cooling requires 3 parameters */
            {
                const char *param = "RadioModeEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "AGNrecipeOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "CoolFunctionsDir";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_disk_instability") == 0) {
            /* sage_disk_instability requires 2 parameters */
            {
                const char *param = "DiskInstabilityOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "DiskRadiusFactor";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_infall") == 0) {
            /* sage_infall requires 1 parameters */
            {
                const char *param = "BaryonFrac";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_mergers") == 0) {
            /* sage_mergers requires 11 parameters */
            {
                const char *param = "AGNrecipeOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "SupernovaRecipeOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FeedbackReheatingEpsilon";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FeedbackEjectionEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "RecycleFraction";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "Yield";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FracZleaveDisk";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "BlackHoleGrowthRate";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "QuasarModeEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "ThreshMajorMerger";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "DiskInstabilityOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_reincorporation") == 0) {
            /* sage_reincorporation requires 1 parameters */
            {
                const char *param = "ReIncorporationFactor";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_satellite_stripping") == 0) {
            /* sage_satellite_stripping requires 1 parameters */
            {
                const char *param = "BaryonFrac";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_starformation_feedback") == 0) {
            /* sage_starformation_feedback requires 11 parameters */
            {
                const char *param = "SFprescription";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "SfrEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "EnergySNcode";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "EtaSNcode";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "SupernovaRecipeOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FeedbackReheatingEpsilon";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FeedbackEjectionEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "RecycleFraction";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "Yield";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FracZleaveDisk";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "DiskInstabilityOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "test_fixture") == 0) {
            /* test_fixture requires 2 parameters */
            {
                const char *param = "TestFixtureDummyParameter";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "TestFixtureEnableLogging";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        }
    }

    *num_required_out = num_required;
    return 0;
}
