/* AUTO-GENERATED CODE - DO NOT EDIT
 *
 * Generated by: scripts/generate_model_parameters.py
 * Source: src/modules/model_parameters.yaml
 * Source MD5: 14effeae0344dea50eb9868bfdb1af4e
 *
 * To regenerate: make generate
 *
 * Vision Principle #4: Single Source of Truth
 *   - model_parameters.yaml defines parameter structure
 *   - module_info.yaml files define parameter dependencies
 *   - Input YAML file provides parameter values (NO defaults)
 *   - All parameters are REQUIRED in input file
 */

#include "generated/model_parameters.h"
#include "error.h"
#include <string.h>
#include <math.h>

/* Required parameter names (for startup validation) */
const char *REQUIRED_MODEL_PARAMETERS[NUM_REQUIRED_MODEL_PARAMETERS] = {
    "BaryonFrac",
    "AGNrecipeOn",
    "CoolFunctionsDir",
    "RadioModeEfficiency",
    "QuasarModeEfficiency",
    "SFprescription",
    "SfrEfficiency",
    "SupernovaRecipeOn",
    "EnergySNcode",
    "EtaSNcode",
    "FeedbackReheatingEpsilon",
    "FeedbackEjectionEfficiency",
    "RecycleFraction",
    "Yield",
    "FracZleaveDisk",
    "ReIncorporationFactor",
    "ThreshMajorMerger",
    "BlackHoleGrowthRate",
    "DiskInstabilityOn",
    "DiskRadiusFactor",
    "TestFixtureDummyParameter",
    "TestFixtureEnableLogging",
};

/* Parameter metadata (for validation) */
const struct ModelParameterMetadata MODEL_PARAMETER_METADATA[NUM_REQUIRED_MODEL_PARAMETERS] = {
    { "BaryonFrac", "double", "Cosmic baryon fraction (Omega_b / Omega_m)", 0.0, 1.0, 1 },
    { "AGNrecipeOn", "int", "AGN feedback mode (0=off, 1=empirical, 2=Bondi-Hoyle, 3=cold cloud accretion)", 0, 3, 1 },
    { "CoolFunctionsDir", "string", "Directory containing cooling function tables (metal-dependent cooling rates)", 0.0, 0.0, 0 },
    { "RadioModeEfficiency", "double", "Efficiency of radio-mode AGN feedback (fraction of accretion energy coupled to gas heating)", 0.0, 1.0, 1 },
    { "QuasarModeEfficiency", "double", "Quasar-mode AGN feedback efficiency (energy coupling to gas during merger)", 0.0, 1.0, 1 },
    { "SFprescription", "int", "Star formation law (0=Kennicutt-Schmidt with density threshold, currently only option)", 0, 0, 1 },
    { "SfrEfficiency", "double", "Star formation efficiency (epsilon_SF in SFR = epsilon_SF * M_cold / t_dyn)", 0.0, 1.0, 1 },
    { "SupernovaRecipeOn", "int", "Enable supernova feedback (0=off, 1=on)", 0, 1, 1 },
    { "EnergySNcode", "double", "Supernova energy per event in code units (E_SN)", 0.0, 100.0, 1 },
    { "EtaSNcode", "double", "Supernova rate per unit stellar mass formed (eta = SNe / M_stars)", 0.0, 10.0, 1 },
    { "FeedbackReheatingEpsilon", "double", "Reheating efficiency (epsilon_reheat) - M_reheat = epsilon_reheat * M_stars", 0.0, 100.0, 1 },
    { "FeedbackEjectionEfficiency", "double", "Ejection efficiency (epsilon_eject) in energy-driven wind formula", 0.0, 100.0, 1 },
    { "RecycleFraction", "double", "Fraction of stellar mass immediately recycled to ISM (instantaneous recycling approximation)", 0.0, 1.0, 1 },
    { "Yield", "double", "Metal yield per unit stellar mass formed (new metals / M_stars)", 0.0, 1.0, 1 },
    { "FracZleaveDisk", "double", "Fraction of newly produced metals that leave the disk in winds (Krumholz & Dekel 2011)", 0.0, 1.0, 1 },
    { "ReIncorporationFactor", "double", "Multiplicative factor for reincorporation timescale (tau_reinc = factor * tau_dyn)", 0.0, 10.0, 1 },
    { "ThreshMajorMerger", "double", "Mass ratio threshold for major mergers (M_sat / M_central > threshold triggers disk destruction)", 0.0, 1.0, 1 },
    { "BlackHoleGrowthRate", "double", "Black hole growth efficiency during mergers (f_BH) - Delta M_BH = f_BH * M_cold", 0.0, 1.0, 1 },
    { "DiskInstabilityOn", "int", "Enable disk instability physics (0=off, 1=on)", 0, 1, 1 },
    { "DiskRadiusFactor", "double", "Factor relating disk effective radius to scale radius (R_eff = factor * R_scale)", 1.0, 10.0, 1 },
    { "TestFixtureDummyParameter", "double", "Test parameter for infrastructure testing only (no physical meaning)", 0.0, 1000.0, 1 },
    { "TestFixtureEnableLogging", "int", "Enable verbose logging in test_fixture module (0=off, 1=on)", 0, 1, 1 },
};

int validate_model_param_double(const char *param_name, double value) {
    const struct ModelParameterMetadata *meta = get_model_param_metadata(param_name);
    if (meta == NULL) {
        ERROR_LOG("Model parameter '%s' not found in metadata", param_name);
        return -1;
    }

    /* Skip validation if no range defined */
    if (!meta->has_range) {
        return 0;
    }

    /* Check range */
    if (value < meta->range_min || value > meta->range_max) {
        ERROR_LOG("Parameter %s = %g out of valid range [%g, %g]",
                  param_name, value, meta->range_min, meta->range_max);
        return -1;
    }

    return 0;
}

int validate_model_param_int(const char *param_name, int value) {
    return validate_model_param_double(param_name, (double)value);
}

const struct ModelParameterMetadata *get_model_param_metadata(const char *param_name) {
    for (int i = 0; i < NUM_REQUIRED_MODEL_PARAMETERS; i++) {
        if (strcmp(MODEL_PARAMETER_METADATA[i].name, param_name) == 0) {
            return &MODEL_PARAMETER_METADATA[i];
        }
    }
    return NULL;
}

/* Smart parameter lookup based on module dependencies */
int get_required_params_for_modules(
    const char **enabled_modules,
    int num_enabled,
    const char **required_params_out,
    int *num_required_out
) {
    if (num_enabled == 0) {
        *num_required_out = 0;
        return 0;
    }

    /* Build union of all required parameters */
    int num_required = 0;

    for (int i = 0; i < num_enabled; i++) {
        const char *module_name = enabled_modules[i];

        if (strcmp(module_name, "sage_cooling") == 0) {
            /* sage_cooling requires 3 parameters */
            {
                const char *param = "RadioModeEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "AGNrecipeOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "CoolFunctionsDir";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_disk_instability") == 0) {
            /* sage_disk_instability requires 2 parameters */
            {
                const char *param = "DiskInstabilityOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "DiskRadiusFactor";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_infall") == 0) {
            /* sage_infall requires 1 parameters */
            {
                const char *param = "BaryonFrac";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_mergers") == 0) {
            /* sage_mergers requires 11 parameters */
            {
                const char *param = "AGNrecipeOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "SupernovaRecipeOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FeedbackReheatingEpsilon";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FeedbackEjectionEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "RecycleFraction";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "Yield";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FracZleaveDisk";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "BlackHoleGrowthRate";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "QuasarModeEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "ThreshMajorMerger";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "DiskInstabilityOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_reincorporation") == 0) {
            /* sage_reincorporation requires 1 parameters */
            {
                const char *param = "ReIncorporationFactor";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_satellite_stripping") == 0) {
            /* sage_satellite_stripping requires 1 parameters */
            {
                const char *param = "BaryonFrac";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        } else if (strcmp(module_name, "sage_starformation_feedback") == 0) {
            /* sage_starformation_feedback requires 11 parameters */
            {
                const char *param = "SFprescription";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "SfrEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "EnergySNcode";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "EtaSNcode";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "SupernovaRecipeOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FeedbackReheatingEpsilon";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FeedbackEjectionEfficiency";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "RecycleFraction";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "Yield";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "FracZleaveDisk";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
            {
                const char *param = "DiskInstabilityOn";
                /* Check if already in list (deduplicate) */
                int found = 0;
                for (int j = 0; j < num_required; j++) {
                    if (strcmp(required_params_out[j], param) == 0) {
                        found = 1;
                        break;
                    }
                }
                if (!found) {
                    required_params_out[num_required++] = param;
                }
            }
        }
    }

    *num_required_out = num_required;
    return 0;
}
